# my-workspace

複数の作業用プロジェクトディレクトリをまとめて管理するリポジトリです。

## 新しいプロジェクトを作成するとき

「my-workspace配下に〇〇というプロジェクト(ディレクトリ)を作って」と依頼された場合は、以下をまとめて行うこと。

1. リポジトリ直下に、指定されたプロジェクト名のディレクトリを新規作成する
2. そのディレクトリ配下に `README.md` を作成し、プロジェクトの概要を記載する
3. リポジトリ直下の `README.md` の一覧に、新しいディレクトリ名と概要を1行追記する
4. 作業用ブランチを作成し、変更をコミットしてpushする
5. GitHub上にPRを作成する(マージはユーザーの指示があるまで行わない)

## 既存プロジェクトを修正するとき

上記の「ディレクトリ作成・README追加」のルールは不要。指示された変更のみを行い、ブランチ作成・コミット・push・PR作成の流れ(上記4, 5)は同様に行う。

## 設計書(docs/)について

- ある程度の規模のアプリ(ログイン・データベース・外部API連携などを伴うもの)を新規作成する際は、
  `jiro-ramen-map/docs/`・`kakeibo/docs/`と同様の設計書一式(要件定義書・基本設計書・詳細設計書・
  環境構築手順書)を作成するかどうかをユーザーに確認すること
- 既存プロジェクトに設計書が無いことに気づいた場合(修正依頼を受けた時など)も、
  「このプロジェクトには設計書が無いが、後から作成する必要があるか」をユーザーに確認すること

## デプロイ済みアプリの動作試験について

「アプリの画面を試験してほしい」と依頼された場合は、以下の方法で行うこと(2026年9月にkakeiboで実施し、良好だった方法)。

1. Playwright(Chromium)でデプロイ済みのURLに対してブラウザ操作を自動実行し、試験項目ごとにスクリーンショットを取得する
   - この環境からデプロイ先URLへ到達できない場合は、ユーザーにネットワーク設定の変更を依頼する
   - この環境のプロキシ用CA証明書が信頼されずTLSエラーになる場合は、Chromium起動時に `--ignore-certificate-errors-spki-list` でそのCAのSPKIハッシュのみを許可する(TLS検証を全体的に無効化しない)
2. 試験結果は**1つのExcelファイル**にまとめる
   - 試験項目書と試験成績書は**同じシート**に統合する(項目・手順・期待結果・実施結果・判定を1行にまとめる)
   - エビデンス(スクリーンショット)は**試験項目ごとにシートを分けて**格納する
3. スクリーンショットは**画面全体(フルページ)**を、マスキングなど加工せずそのまま貼り付ける
4. 注目してほしい箇所は、画像自体には手を加えず、**Excelの図形(塗りつぶし無し・赤枠線)を画像の上に重ねて**強調する
5. Excelファイルは、この環境内でPythonスクリプト(openpyxl等)を使って直接組み立てる
   - チャットやAPI経由でスクリーンショット画像をテキスト(base64)としてやり取りする方法は、数KB程度でも内容が壊れることがあり、時間もかかるため使わないこと
   - 完成したExcelファイルは`SendUserFile`でユーザーに直接渡す。Google Driveなど外部サービスへのアップロードは、ファイルサイズがチャット経由のテキスト転送に収まらないため、基本的にユーザー自身にお願いする
6. 試験のためにデプロイ先の本番データベースへ追加したデータ(テスト用の記録・カテゴリなど)は、試験終了後に削除し、元の状態に戻す

## PRマージ後の案内

GitHub Pages(`https://masaharushibata.github.io/my-workspace/`)が有効なため、`index.html` を持つ静的サイトのプロジェクト(例: `portfolio`, `calculator`)に関するPRをマージしたときは、その直後に以下を案内すること。

- 動作確認用URL: `https://masaharushibata.github.io/my-workspace/<ディレクトリ名>/`(末尾のスラッシュを忘れないこと)
- GitHub Pagesの再ビルドに1〜2分ほどかかる場合がある旨

サーバー側の処理を伴うプロジェクト(GitHub Pagesでは動作確認できないもの)の場合は、この案内は不要。

## その他の方針

- コミットメッセージ・PR説明文は日本語で簡潔に書く
- デザインや文章を新規作成する場合、テンプレートっぽさ・AI生成っぽさを避け、自然で洗練された仕上がりを意識する
