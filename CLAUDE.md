# my-workspace

複数の作業用プロジェクトディレクトリをまとめて管理するリポジトリです。

## 新しいプロジェクトを作成するとき

「my-workspace配下に〇〇というプロジェクト(ディレクトリ)を作って」と依頼された場合は、以下をまとめて行うこと。

1. リポジトリ直下に、指定されたプロジェクト名のディレクトリを新規作成する
2. そのディレクトリ配下に `README.md` を作成し、プロジェクトの概要を記載する
3. リポジトリ直下の `README.md` の一覧に、新しいディレクトリ名と概要を1行追記する
4. 作業用ブランチを作成し、変更をコミットしてpushする
5. GitHub上にPRを作成する(マージはユーザーの指示があるまで行わない)

## 既存プロジェクトを修正するとき

上記の「ディレクトリ作成・README追加」のルールは不要。指示された変更のみを行い、ブランチ作成・コミット・push・PR作成の流れ(上記4, 5)は同様に行う。

## 設計書(docs/)について

- ある程度の規模のアプリ(ログイン・データベース・外部API連携などを伴うもの)を新規作成する際は、
  `jiro-ramen-map/docs/`・`kakeibo/docs/`と同様の設計書一式(要件定義書・基本設計書・詳細設計書・
  環境構築手順書)を作成するかどうかをユーザーに確認すること
- 既存プロジェクトに設計書が無いことに気づいた場合(修正依頼を受けた時など)も、
  「このプロジェクトには設計書が無いが、後から作成する必要があるか」をユーザーに確認すること

## 製造・環境構築で気をつけること

Supabase・Vercel・外部API(Claude APIなど)を使うアプリの製造・環境構築時、以下の問題が実際に発生した
(2026年9月、my-recipeの開発時)。同様の構成で開発する際は事前に踏まえておくこと。

- **Supabaseの無料プランはプロジェクト数に上限がある**(組織あたりアクティブ2件など)。新規プロジェクト
  作成でこの上限に達した場合、既存プロジェクトの削除ではなく**一時停止(pause)**で枠を空けることを
  検討する(SupabaseのMCP接続には「完全削除」のツールが無く、「一時停止」のみ提供されている。
  一時停止はデータを消さず後で復元できるため、削除より安全)。ただし対象が他プロジェクトの本番データを
  含む可能性があるため、**必ず影響範囲をユーザーに明示し、確認を取ってから**実行する
- **Vercelでプロジェクトを新規作成するには「チーム」が必要**。個人アカウント単体(チーム無し)では、
  MCP経由でのプロジェクト作成・デプロイ関連ツールの多くが使えない仕様になっている。ユーザーのVercel
  アカウントにチームが無い場合は、無料(Hobbyプラン)でチームを作成してもらう(チーム作成自体は無料。
  作成時にProプランを案内されても選ばなくて良い)
- チームを新規作成した直後は、Claude側のVercelコネクタがそのチームをまだ認識しないことがある(認可
  時点のチーム構成がキャッシュされているため)。その場合はユーザーに**コネクタの切断→再接続**を
  依頼すると解決する
- VercelのMCPコネクタには、環境変数(Environment Variables)を登録・変更するツールが存在しない
  (2026年9月時点)。`ANTHROPIC_API_KEY`等の環境変数は、Vercelダッシュボードからユーザーに登録して
  もらう必要がある。環境構築手順書にもこの制約を明記し、事前にユーザーへ伝えておくこと
- **Vercelのデプロイ固有URL**(`https://<project>-<hash>-<team>.vercel.app`のような、ハッシュを含む
  形式)には、Vercelログイン(SSO)保護がデフォルトでかかっており、一般公開されない。動作確認・試験には
  必ず安定した本番ドメイン(`https://<project>-xxxx.vercel.app`のようなプロジェクト単位のドメイン、
  またはカスタムドメイン)を使うこと

## デプロイ済みアプリの動作試験について

「アプリの画面を試験してほしい」と依頼された場合は、以下の方法で行うこと(2026年9月にkakeibo・
my-recipeで実施し、確立した手順)。

### 1. Playwrightによる自動操作

- Playwright(Chromium)でデプロイ済みのURLに対してブラウザ操作を自動実行し、試験項目ごとにスクリーン
  ショットを取得する
- この環境からデプロイ先URLへ到達できない場合は、ユーザーにネットワーク設定の変更を依頼する
- この環境のプロキシ用CA証明書が信頼されずTLSエラーになる場合は、Chromium起動時に
  `--ignore-certificate-errors-spki-list` でそのCAのSPKIハッシュのみを許可する
  (TLS検証を全体的に無効化しない)

### 2. Vercelの自動保護(レート制限・異常検知)への対策 ★重要

自動操作による短時間の連続アクセスは、Vercel(特にHobbyプラン)の自動レート制限/異常検知(Firewall)を
誤って発動させることがある(my-recipeの試験で実際に発生し、一時ほぼ全リクエストが403になった)。
以下を必ず踏まえて実施すること。

- **リクエスト間隔を空ける**: 画面遷移・操作の間に2〜4秒程度、リトライが必要な場合は最低30〜60秒の
  間隔を空ける。**短時間の連続リトライは状況を悪化させる**ため厳禁(悪化した場合、回復まで待つしかない)
- **ハイドレーション確認**: ページの静的アセット(JS/CSSチャンク)が403等で部分的に読み込み失敗すると、
  画面は正しく表示されるのにクリックなどの操作が一切効かない「ハイドレーション未完了」状態になることが
  ある。操作前に、Reactのイベントハンドラが実際に紐づいているか確認してから進める。

  ```js
  const hydrated = await page.evaluate(() => {
    const el = document.querySelector("button, input");
    return el
      ? Object.keys(el).some(
          (k) => k.startsWith("__reactProps$") || k.startsWith("__reactEventHandlers$")
        )
      : false;
  });
  ```

  未完了の場合は再読み込みして再確認する(即時リトライではなく数十秒空ける)
- **403が続く場合**: `curl -s -o /dev/null -w "%{http_code}\n" <URL>` を数回実行して状況を確認し、
  改善しない場合はユーザーに以下を依頼する。
  1. 自分の端末から同じURLにアクセスできるか確認してもらう(この環境固有の問題かを切り分ける)
  2. Vercelダッシュボードの「Firewall」タブで「Denied」になっているアクセスの有無を確認してもらう
  3. この環境の送信元IPを`curl https://api.ipify.org`で複数回取得する(**固定IPではなく`/24`程度の
     範囲でローテーションすることが多い**)。確認できた範囲をCIDR表記でVercel Firewallの
     「バイパス」ルールに登録してもらう(単一IPの登録では不十分な場合がある)
- **デプロイ固有URLは使わない**: 上記「製造・環境構築で気をつけること」を参照。試験には必ず安定した
  本番ドメインを使う

### 3. 試験結果のExcel化(フォーマット統一)

試験結果は**1つのExcelファイル**にまとめる。プロジェクトが変わっても同じフォーマットになるよう、
以下の仕様に統一すること。

**ファイル名**: `<プロジェクト名>_試験成績書_<実施日 YYYYMMDD>.xlsx`
(例: `my-recipe_試験成績書_20260918.xlsx`)

**シート構成**:
- 1枚目: シート名は固定で `試験項目書_試験成績書`。試験項目書と試験成績書を**同じシート・同じ行**に
  統合する(列: `No` / `試験項目` / `試験手順` / `期待結果` / `実施結果` / `判定`)
- 2枚目以降: 1シート1試験項目のエビデンス。シート名は`No`列の値と完全に一致させる
  (例: `TC01`, `TC02`...)。`No`は`TC`+2桁連番(`TC01`〜)で固定する

**配色・書式(固定・プロジェクトのブランドカラーに関わらず統一する)**:
- 1枚目タイトル行: 太字・14pt
- 見出し行(`No`〜`判定`): 太字、背景色 `#E8ECEF`(薄いグレー)、中央揃え、枠線あり
- 通常セル: 枠線あり、上詰め、折り返し表示
- 判定列: `OK`は太字・緑(`#147A5B`)、`NG`は太字・赤(`#C1452C`)、いずれも中央揃え
- 備考・注記行: イタリック、グレー文字(`#7C6D59`)

**エビデンスシート(1シート1項目)**:
- スクリーンショットは**画面全体(フルページ)**を、マスキングなど加工せずそのまま貼り付ける
  (Excel上での縮小表示は可。元画像ファイル自体は無加工のまま)
- 注目してほしい箇所は、画像自体には手を加えず、**Excelの図形(塗りつぶし無し・赤枠線`#E4342A`・
  線幅2.5pt)を画像の上に重ねて**強調する
- openpyxlには通常のワークシート上に図形(塗りなしの矩形など)を追加する公開APIが無いため、
  `xlsxwriter`の`insert_image`(画像)と`insert_textbox`(テキスト無しの矩形として、塗りなし・枠線のみ
  を指定)を組み合わせて実現する
- 図形の位置は、画像に適用した縮小率(`x_scale`/`y_scale`)と同じ倍率を、ハイライトしたい範囲
  (元画像のピクセル座標)にも掛けて`x_offset`/`y_offset`/`width`/`height`を算出し、画像とシェイプを
  同じセルにアンカーする(こうすることで縮小後も位置がずれない)

**その他**:
- Excelファイルは、この環境内でPythonスクリプトを使って直接組み立てる
  - チャットやAPI経由でスクリーンショット画像をテキスト(base64)としてやり取りする方法は、数KB程度
    でも内容が壊れることがあり、時間もかかるため使わないこと
  - 完成したExcelファイルは`SendUserFile`でユーザーに直接渡す。Google Driveなど外部サービスへの
    アップロードは、ファイルサイズがチャット経由のテキスト転送に収まらないため、基本的にユーザー
    自身にお願いする
- 生成したExcelファイルは、可能であれば`libreoffice --headless --convert-to pdf`等で実際に開けるか
  検証する。**この環境のLibreOfficeが変換自体できない(壊れている)場合がある**(単純な空のxlsxすら
  変換できないことがある)。その場合は、変換失敗がファイル固有の問題か環境側の問題かを、
  空ファイルでの再現テストで見極めた上で、openpyxlでの再読み込み・zip整合性チェック
  (`ZipFile.testzip()`)・各XMLパートのwell-formed検証で代替する
- 試験のためにデプロイ先の本番データベースへ追加したデータ(テスト用の記録・カテゴリなど)は、
  試験終了後に削除し、元の状態に戻す

## 教材コンテンツ(音声版・動画版)を作るとき

チュートリアル等の教材を、音声や動画で学べる形に派生させることがある(2026年9月、
my-recipeで実施)。同様の依頼を受けたときは以下を踏まえること。

### 音声版(読み上げアプリ用のmdファイル)

- **1ファイルにまとめる**。章ごとに分割するとアプリ側で扱いづらい
- 見出しと本編へのリンク以外、**マークダウン記号を使わない**(太字・コードの囲み・
  箇条書き記号・表は、読み上げアプリが記号をそのまま読んでしまう)
- コードブロックは載せず、「何をする処理か」を言葉で説明する。図・表は文章に言い換える
- 記号を含む名前には読みを添える(`NEXT_PUBLIC_` →「ネクスト・アンダーバー・パブリック」)
- 各章の冒頭に本編へのリンクを1行置く。**GitHubの絶対URL**にすること
  (ファイルをスマホへコピーした後でも開けるようにするため)

### 動画版(スライド+ナレーション)

仕組み一式は `my-recipe/video/` にある。台本の記法など細かい仕様は
`my-recipe/video/README.md`、作業途中の申し送りは `my-recipe/video/HANDOVER.md` を参照。
他プロジェクトで作る場合も、この構成をディレクトリごと流用する。

**台本(YAML)から生成する。** 1スライド=「画面に出す内容」+「ナレーション」。
ナレーションがそのまま音声と字幕になるので、**文言だけ直して再生成できる**のが要。
パイプラインは、Playwright(Chromium)でスライドをPNG化 → `edge-tts`で音声合成 →
`ffmpeg`で結合、の3段。

#### 作る手順

コンテナは破棄されるので、セッションをまたぐときは毎回1からやり直しになる。
この順で進めること。

1. **セットアップ**(`my-recipe/video/` で実行)
   ```
   npm install                      # ffmpeg-static / playwright-core
   pip install pyyaml edge-tts
   ```
   続けて `certifi` にプロキシのCAを追記する(後述の制約を参照)。
   フォントは `build.py` の `ensure_fonts()` が初回に自動取得する。
2. **台本を書く**(`chapters/<章番号>_<英名>.yaml`)
3. **実機デモを撮る**(`kind: demo` を使う章のみ。後述)
4. **ビルド**
   ```
   python3 scripts/build.py chapters/01_web-app-overview.yaml
   ```
   台本を書きながら見た目だけ確認したいときは `--slides-only --only 10`。
5. **出来上がりを確認する**(後述)
6. **`SendUserFile` で渡す**。動画と字幕(SRT)を一緒に渡す

**必ず1章だけのパイロットを作り、確認を取ってから量産する**(声・テンポ・情報量・尺)。
2章以降をまとめて作るのは、ユーザーから明示的に依頼されてから。

**1章につき1セッション。** 1つのセッションで複数章を作らないこと。原本を読んで台本を
書くだけで相当量のコンテキストを使うため、章をまたぐと後半の品質が落ちる。
どの章まで進んだかは `my-recipe/video/PROGRESS.md` に記録する。

#### 台本の構成

- **第1章は「1章の解説」ではなく「全章の入口」として作る**。章の中身に入る前に、
  ①何を作るのか(実機デモを流す)②全何章でそれを作るのか ③章立てと進め方、をこの順で
  済ませること。これを飛ばすと、表現がどれだけキャッチーでも
  「結局この動画は何なのか分からない」ものになる(2026年9月、実際に指摘を受けた)
- **掴みだけを尖らせない**。センセーショナルな導入(「請求69万円」など)は、
  冒頭のフックではなく**本題の中の具体例**として使う。視聴者が
  「何の話を聞かされているのか」を先に分かっている状態を作るほうが優先
- 章の中身は、**原本の節の順番をなぞらない**。「この章で答える問い」を2〜3個立て、
  それに順番に答える形へ組み替える。問いの一覧を節目ごとに再掲し、
  済んだものに印を付けると現在地が分かる
- **終盤で冒頭を回収する**。第1章なら、冒頭で流したデモを裏側から説明し直して閉じる
- 最後に次章の予告を置く
- 尺は1章あたり**10〜15分**を目安にする。超えそうなら、原本にはあるが
  口頭で聞いて分かりにくい部分(細かい表・列挙)から削る

#### スライドとナレーションの書き方

- **1画面1メッセージ。文字は極力減らす**。箇条書き(`points`)は原則使わず、
  図・対比・大きな一言に置き換える。読ませるのではなく、聞かせて見せる
- **絵文字を使わない**。フォント依存で化ける。図版はインラインSVGのアイコンで描く
- 配色はアプリ本体のデザイントークンを流用する。教材とアプリで世界観が揃う
- ナレーションは**話し言葉**で書く。1文を短く切ると、字幕も段階表示もきれいに揃う
- **記号を含む名前には読みを書く**。`NEXT_PUBLIC_` は
  「ネクスト・アンダーバー・パブリック・アンダーバー」と書く(音声版と同じ方針)
- スライドの要素は**ナレーションの文の切れ目に合わせて順に現れる**(段階表示)。
  そのため、見せたい要素の数と文の数を揃えておくと絵と話が噛み合う。
  一度に全部見せたいスライドだけ `reveal: false` を付ける

#### 実機デモを差し込むとき

デモがあると「何を作るのか」が一目で伝わる。第1章には必ず入れる。

- **本番のアプリは撮らない**。APIの課金が発生し、本番DBにテストデータが入り、
  Vercelのレート制限を誘発する。`my-recipe/video/demoapp/setup.sh` が、アプリ一式を
  作業用ディレクトリへコピーし、外部サービスを呼ぶ層(`lib/claude.ts`・
  `lib/supabase/server.ts`)**だけ**をスタブに差し替えて起動する。
  画面・CSS・操作は実物のままなので「実際のアプリ」と言える
- 撮影は `scripts/capture-demo.mjs`。シーンが部品になっていて、`--scenes` で
  章ごとに必要なものだけ撮れる。**絞っても操作の流れは最初から最後まで通る**
  (レシピが出ていないとお気に入り登録ができない、といった前提があるため)
- 新しいアプリで一式を作るときの勘どころ
  - スタブにした分、`package.json` から対応する依存(SDK等)を外すとインストールが軽い
  - 偽DBには最初から数件入れておく。一覧が空だと絵にならない
  - AIの呼び出しは**あえて3秒ほど待たせる**。待ち時間ごと映さないと
    「裏で何かが起きている」ことが伝わらない
  - Next.jsなら`devIndicators: false`を入れる。開発中バッジが画面に写り込む
- 拡大は`transform: scale()`ではなく、アプリ文書側の`zoom`で行う。`transform`だと
  Playwrightのクリック位置計算がずれ、`<main> intercepts pointer events`で操作不能になる
- アプリは**ブラウザ風の枠を描いたシェルHTMLにiframeで埋めて、枠ごと連写する**。
  枠・見出し・操作の説明キャプションが最初から入るので、出てきたPNGをそのまま
  スライドのコマとして使える。クリック位置には輪を重ねると、どこを押したか伝わる
- **シーンの長さとナレーションの長さを合わせる**。コマは尺に合わせて等間隔に
  引き伸ばされるため、ナレーションが長すぎるとスロー再生になる。文字入力や
  スクロールが入るシーンは実時間の1.2倍が限度(ほぼ静止した画面なら2倍でも分からない)。
  合わないときは、台本を削るのではなく**撮影側の待ち時間を足して撮り直す**

#### 出来上がりの確認

目視だけで済ませない。最低限これを確認してから渡すこと。

- **壊れていないか**: `ffmpeg -v error -i <mp4> -f null -` を通し、エラーが出ないこと
  (長いエンコードが途中で切られると `moov atom not found` の壊れたMP4ができる)
- **仕様どおりか**: 解像度1920×1080、映像h264・音声aacが両方入っていること
- **中身**: `ffmpeg -ss <時刻> -i <mp4> -frames:v 1 out.png` で数か所を抜き、
  新しく作ったスライドが意図どおり描けているかを実際に見る
- **字幕**: SRTの件数と、先頭が音声とずれていないこと
- **デモの速度**: 各デモシーンのコマ数 ÷ 12fps と、そのスライドのナレーション尺を比べる
  (`build.py` の出力にスライドごとの秒数が出る)

#### 渡し方

動画ファイルは**Git管理しない**。1章で十数MB、全章で1GB弱になり履歴から消せなくなる。
台本と生成スクリプトだけをコミットし、完成品は`SendUserFile`で直接渡す。
`out/`・`.fonts/`・`node_modules/`・`__pycache__/` は `.gitignore` に入れておく。

### ⚠️ この環境で実際に踏んだ制約(動画生成)

- **`edge-tts`はプロキシのCA証明書を読まない**。`ssl.create_default_context(cafile=certifi.where())`
  を直接呼ぶ実装のため、`SSL_CERT_FILE`等の環境変数が効かない。
  `certifi`のバンドルに`/root/.ccr/ca-bundle.crt`を**追記**すれば通る。
  このとき「もう入っているか」を雑に判定しないこと。`/root/.ccr/ca-bundle.crt`は
  **システムのルート証明書一式＋プロキシのCA**という構成のため、先頭の証明書だけを見て
  判定すると必ず「入っている」と誤判定する。証明書ブロック単位で比較し、
  足りないものだけ追記する(実測で30件が不足していた)
- **Playwright同梱のffmpegは使えない**。VP8/webmのみで音声エンコーダを持たない。
  `npm install ffmpeg-static` で libx264 + aac が入ったものを別途用意する
- **スライドの描画はPlaywright経由で行う**。Chromiumを直接`--screenshot`で叩くと、
  ビューポートが`--window-size`より小さくなり(実測で87px)、スライド下端が写らない。
  Playwrightの`newPage({ viewport })`なら常に指定どおりのサイズになる
- **日本語の音声は`WordBoundary`を返さない**。`SentenceBoundary`が返るので、
  字幕はそちらから組み立てる(結果的に文単位のきれいな字幕になる)
- **日本語フォントはIPAGothicしか入っていない**。Noto Sans JPを使うなら、
  Google FontsのCSS API経由で`fonts.gstatic.com`から取得する
  (GitHubのrawはセッションのリポジトリ制限で取得できない)
- 音声合成は遅く外部サービスを叩くため、**台本が変わっていなければ使い回すキャッシュ**を持たせる
  (`out/<章>/s###.cues.json`。ナレーション本文と声の両方が一致したスライドだけ再利用される)。
  ただし `out/` はGit管理外なので、**セッションが変わると全スライド合成し直し**になる。
  43スライドで数分かかる前提で見ておくこと
- **バックグラウンドで長い処理を走らせると、途中で切られることがある**。
  ビルドは前面で走らせ、完了ログ(「完成」)を確認してから次へ進む

### ⚠️ 音声合成の利用条件

`edge-tts`はMicrosoft Edgeの読み上げ機能を利用するもので、**個人の学習用途を前提**とする。
公開配布・商用を検討する場合は、正規のサービス(Azure Speech、Google Cloud TTS等。
APIキーと課金が必要)へ差し替える。差し替えが1関数で済むよう分離しておくこと。

## PRマージ後の案内

GitHub Pages(`https://masaharushibata.github.io/my-workspace/`)が有効なため、`index.html` を持つ静的サイトのプロジェクト(例: `portfolio`, `calculator`)に関するPRをマージしたときは、その直後に以下を案内すること。

- 動作確認用URL: `https://masaharushibata.github.io/my-workspace/<ディレクトリ名>/`(末尾のスラッシュを忘れないこと)
- GitHub Pagesの再ビルドに1〜2分ほどかかる場合がある旨

サーバー側の処理を伴うプロジェクト(GitHub Pagesでは動作確認できないもの)の場合は、この案内は不要。

## その他の方針

- コミットメッセージ・PR説明文は日本語で簡潔に書く
- デザインや文章を新規作成する場合、テンプレートっぽさ・AI生成っぽさを避け、自然で洗練された仕上がりを意識する
